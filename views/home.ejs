<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <!-- para gumana yung css nagdeclare tayo ng static folder which is nandon yung css natin -->
    <link rel="stylesheet" href="/style.css" /> 
</head>
<body>
    Welcome to the homepage
    <h1>What color is the sky on a clear day?</h1>
    <form action="/result" method="POST">
        <input type="text" name="color">
        <button>Submit Answer</button>
    </form>

    <% if(isRaining){ %>
        <p>It is raining. It is not raining.</p>
    <% } else { %>
        <p>It is not raining.</p>
    <% } %>
    
    <% pets.forEach(pet => { %>
        <li><%= pet.name %> (<%= pet.species %>)</li>
    <% }) %>

</body>
</html>